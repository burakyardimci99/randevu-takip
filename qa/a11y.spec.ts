/**
 * TAŞINDI → frontend/e2e/a11y.spec.ts
 *
 * Bu spec artık standart e2e paketiyle birlikte (CI dahil) koşar:
 *   cd frontend && npm run e2e
 *
 * qa/ dizininden bağımsız çalıştırılamıyordu (node_modules çözümlemesi);
 * frontend/e2e altına alındı. Bu dosya yalnız yönlendirme amaçlı bırakıldı.
 */
